# MOUSE-NEXT Agent 上下文

系统级鼠标手势工具（Rust / Windows）。在系统层面监测鼠标按键与轨迹，识别方向并映射为键盘快捷键，控制浏览器 tab 等。

**当前进度**：输入监测、动作模块、egui overlay 手势 UI 已可用。方向唯一由 overlay 邻格命中产生；旧位移轨迹监测链路已移除。UI 为三格常驻（左/中/右）水平布局，iOS 扁平风格，触碰左右格绿色激活后缓慢褪色。左/右 tab 切换已接入 PrintWindow 冻结层。白名单、Ctrl 按住/松开、真实 tab 数据尚未实现。

**当前目标**：继续打磨三格 UI 动画与手感，保持简单可靠；不扩展真实 tab 数据、浏览器扩展或复杂菜单系统。

**计划状态**：当前阶段优先保持同步线程 + `std::sync::mpsc` 的简单可靠风格；Tokio 不在本阶段引入。

## 当前计划待办

目标：三格常驻 UI 稳定运行，手势期间可靠显示格子，不抢焦点、不破坏快捷键输入。

- [x] 接入 `eframe/egui` overlay
- [x] 改为 3×3 动态九宫格（已废弃，替换为三格常驻）
- [x] 三格常驻水平布局：左/中/右，中格完全不透明，左右半透明
- [x] 触碰左右格：iOS 绿色激活（#34C759），缓慢褪色回白/灰底
- [x] 方向触发后三格滑动到新位置，旧边界外格子淡出，新格子淡入
- [x] 上下方向隐形检测，触发方向但不影响格子显示
- [x] iOS 扁平纯色风格，无玻璃/阴影/高光
- [x] 窗口重定位帧跳过绘制，消除闪现
- [x] tab 切换冻结层：PrintWindow 快照、延迟缓存、像素对齐绘制、指数淡出
- [ ] 动作名显示在格子上（当前仅纯色无文字）

## 已确认设计偏好

- 简单可靠优先：核心输入链路继续使用同步线程和 channel，不为当前浮窗提前引入 Tokio。
- Hook 低层稳定：Hook 只采集 Windows 鼠标事件、判断右键吞掉/透传、发送事件，不维护浮窗状态、不做手势识别。
- 方向触发在 overlay：鼠标进入邻格区域时 overlay 发送 `方向`，驱动动作表；匹配器消费的是方向事件序列（如 下→右），非鼠标位移。
- UI 不参与动作匹配逻辑，只负责邻格命中检测、视觉反馈和方向事件转发。
- UI 消费原始坐标控制打开/关闭与邻格命中。
- 左右格始终可见（半透明），提供常驻视觉提示；上下方向隐形。
- 右键抬起关闭浮窗并发送 `方向::结束`；本会话未触发手势则补发右键单击。
- 后续外部数据模块独立，不直接侵入 Hook 或手势处理器。
- 文档保留高层上下文、关键取舍；具体实现细节优先放在代码结构和必要注释里。
- 修改应贴合当前代码风格，避免为未来可能性提前引入重架构。
- overlay 窗口机制的详细实验记录见 [overlay-window-mechanism.md](overlay-window-mechanism.md)。

## 架构

四线程单向事件流：

```
Hook 线程 ──原始事件 channel──► Overlay 输入线程
  WH_MOUSE_LL 吞/透传              │ 邻格命中 → 方向 channel
                                   ▼
                           匹配器线程 ◄── 方向::开始/结束/上/下/左/右
                           方向队列 + 触发序列匹配
                                   │
                                   ▼
主线程（egui 窗口）◄── 重绘信号 ── 执行 queue ──► 执行器线程
                                   SendInput
```

- Hook 回调只读 Windows 事件、转发到单一 channel、决定吞掉或透传；不维护状态、不做手势识别
- overlay 在 开始~结束 会话内，由邻格命中发送方向；匹配器按 `触发序列` 前缀匹配动作
- 本会话未触发手势时，overlay 在浮窗关闭后延迟补发右键单击；已触发手势则不补发
- 匹配器在 开始~结束 会话内匹配动作；独占动作触发后会话锁定至 结束
- 执行器将匹配到的按键序列通过 `SendInput` 发送

## 浮窗 UI

三格常驻水平布局，egui 浮窗负责 UI 与方向触发，不参与动作匹配。

```
Hook 线程
  └── 原始鼠标事件 -> Overlay 状态 -> egui 透明浮窗
                              │
                              └── 邻格命中 -> 方向 channel -> 匹配器 -> 执行器
```

- 右键按下：显示左/中/右三格，发送 `方向::开始`。中格白底完全不透明，左右格浅灰底半透明。
- 移入左/右邻格：该格变 iOS 绿色，发送对应方向，三格滑动到新位置。旧边界外格子淡出，新方向格子淡入。
- 上下邻格：隐形检测，触发方向但不影响三格显示。
- 右键抬起：关闭浮窗，发送 `方向::结束`；若本会话未触发手势，延迟补发原位置右键单击。
- 浮窗窗口：空闲 `1x1` 隐藏，手势期间放大到工作区 99%（居中）。
- tab 冻结层（左/右）：`PrintWindow` 截前台窗口；会话开始与每次方向触发后延迟 50ms 刷新缓存；切 tab 时立即 SendInput，若有缓存则显示快照并按屏幕坐标 1:1 绘制（超出 overlay 裁剪）；指数淡出。

## 关键实现原则

- 浮窗窗口：egui 负责绘制，Win32 负责几何切换和不抢焦点；输入与重绘均由 hook 事件驱动。
- 通道：`std::sync::mpsc` 单消费者；Hook 仅向 Overlay 输入线程发送原始事件。
- Tokio：不是当前浮窗阶段的基础依赖。
- 线程模型：Hook、Overlay 输入、匹配器、执行器、主线程 egui 各自保持清晰边界。

### 预期模块结构

```
src/
├── overlay/
│   ├── mod.rs       # Overlay 模块入口
│   ├── state.rs     # 三格状态机、邻格检测、方向触发、动画推进
│   ├── animation.rs # 视觉辅助（当前精简）
│   ├── capture.rs   # PrintWindow 截屏、延迟缓存刷新
│   ├── input.rs     # 消费 hook 事件，转发方向到匹配器
│   ├── repaint.rs   # 跨线程 request_repaint
│   └── window.rs    # egui 绘制、iOS 扁平风格、Win32 几何切换
└── external/        # 后续外部数据服务，可选
    ├── browser.rs
    ├── vscode.rs
    └── explorer.rs
```

## 后续方向

- 外部数据服务：后续浏览器扩展、VSCode、Windows 资源管理器 tab 列表获取，建议独立成模块。
- 局部 Tokio：如果外部数据服务需要异步 I/O，可在对应模块内部启动 Tokio runtime；对主程序仍只暴露普通 channel 接口。
- 核心链路保持同步：Hook、手势处理、动作匹配、SendInput、Overlay 状态更新继续优先使用同步线程和 channel。
- UI 形态可演进：当前为三格常驻 + 滑动动画，后续可替换为 tab 列表选择器或类似 Alt+Tab 的切换面板。

## 命名规范

- 业务类型 / 函数 / 变量：主要用中文
- Win32 常量 / API、channel / Hook 等惯用词：保留英文
- 文件名 / 模块路径：英文（Rust 惯例）

## 文件树

```
mouse-next/
├── Agents.md              # 本文件，首轮对话上下文
├── mouse-next.md          # 需求与功能规格（权威来源，随开发更新）
├── Cargo.toml             # Rust 项目配置与依赖
├── Cargo.lock             # 依赖锁定
├── .gitignore             # 忽略 /target
└── src/
    ├── main.rs            # 程序入口
    ├── config.rs          # 格边长步长、冻结层等全局配置
    ├── input/             # 输入监测模块
    │   ├── mod.rs
    │   ├── types.rs
    │   ├── hook.rs
    │   └── replay.rs
    ├── action/            # 动作模块
    │   ├── mod.rs
    │   ├── types.rs
    │   ├── registry.rs    # 动作配置表
    │   ├── preview.rs     # overlay 动作预览
    │   ├── matcher.rs
    │   └── executor.rs
    └── overlay/           # egui 浮窗模块
        ├── mod.rs
        ├── state.rs       # 三格状态机、邻格检测、动画推进
        ├── animation.rs   # 视觉辅助
        ├── capture.rs     # PrintWindow 截屏
        ├── input.rs       # 消费 hook 事件
        ├── repaint.rs     # 跨线程 request_repaint
        └── window.rs      # egui 绘制、窗口管理
```
