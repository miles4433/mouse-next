# MOUSE-NEXT 全局鼠标手势工具
鼠标手势在国产浏览器中是内置原生功能,但chrome原生没有,以及考虑到扩展实现的性能 效果不好(比如页面打开时 系统页面扩展没有输入响应)
所以需要一个软件,在系统层面检测鼠标按键和移动输入,获取鼠标轨迹,并转换成对应的键盘按键映射,通过按键映射或者扩展传递去控制实际页面

## 本文档说明
本文档用于记录开发需要的各种需求功能细节等,需要随时调整并确保所有信息的绝对准确

## 输入输出

### 输入
- 鼠标按键
- 鼠标轨迹

### 输出
- 键盘按键

## 使用说明功能效果
在白名单程序中,监测输入并映射为输出,实现tab页面的控制

### 浏览器 vscode 资源管理器
按住鼠标右键 -> ctrl按住
鼠标右滑一小段 -> tab
鼠标左滑一小段 -> shift + tab
松开鼠标右键 -> ctrl松开

## 后面的方向

### 管理界面浮窗
需要实现一个临时的浮窗 用来同时展示多个tab,然后进行切换 选择或者其他功能
效果上类似win的alt+tab的这种

# 功能模块

## 输入监测模块

注册WH_MOUSE_LL HOOK
接收到的事件带注入标志,跳过处理
接收到触发按键的事件,吞掉并处理(如鼠标右键)
接收到鼠标位移事件,处理但不吞掉

### 处理函数

#### 开始记录轨迹
接收到鼠标右键按下时,切换状态机
通过channel发送开始事件

#### 结束记录轨迹
接收到鼠标右键抬起时,切换状态机
重置之前维护的值
通过channel发送结束事件
传递普通右键单击:
右键的按下和抬起已经被吞掉,但是在没有触发轨迹事件时,这里抬起时要补发完整的右键按下抬起事件

#### 轨迹位移积累
维护 刚刚/现在的两套x,y坐标
维护 带符号的x,y累积变量(如果方向反了也会反向积累或者抵消)
x,y达到方向阈值,重置x,y累积并通过channel发送上下左右事件之一


### 输出
通过channel向外发送监测事件:
- 开始: 右键按下
- 结束: 右键抬起
- 上/下/左/右: 轨迹方向
