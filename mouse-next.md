# MOUSE-NEXT 全局鼠标手势工具
鼠标手势在国产浏览器中是内置原生功能,但chrome原生没有,以及考虑到扩展实现的性能 效果不好(比如页面打开时 系统页面扩展没有输入响应)
所以需要一个软件,在系统层面检测鼠标按键和移动输入,获取鼠标轨迹,并转换成对应的键盘按键映射,通过按键映射或者扩展传递去控制实际页面

## 本文档说明
本文档用于记录开发需要的各种需求功能细节等,需要随时调整并确保所有信息的绝对准确

## 输入输出

### 输入
- 鼠标按键
- 鼠标轨迹

### 输出
- 键盘按键

## 使用说明功能效果
在白名单程序中,监测输入并映射为输出,实现tab页面的控制

### 浏览器 vscode 资源管理器
按住鼠标右键 -> ctrl按住
鼠标右滑一小段 -> tab
鼠标左滑一小段 -> shift + tab
松开鼠标右键 -> ctrl松开

## 后面的方向

### 管理界面浮窗
需要实现一个临时的浮窗 用来同时展示多个tab,然后进行切换 选择或者其他功能
效果上类似win的alt+tab的这种

### 手势 overlay
当前使用 egui/eframe 实现透明浮窗。右键按下展开 3×3 动态九宫格，移入上/下/左/右格触发方向并重建宫格；动作方向由 overlay 宫格命中产生，不再使用轨迹距离阈值驱动匹配器。

空闲时 `1x1` 隐藏，手势期间放大到工作区 99%（居中），由 hook 事件驱动重绘。完全贴满工作区会导致浏览器 tab 切换后不刷新。详见 `overlay-window-mechanism.md`。

# 功能模块

## 输入监测模块

注册WH_MOUSE_LL HOOK
接收到的事件带注入标志,跳过处理
接收到触发按键的事件,吞掉并处理(如鼠标右键)
接收到鼠标位移事件,处理但不吞掉

### 处理函数

#### 开始记录轨迹
接收到鼠标右键按下时,切换状态机
通过channel发送开始事件

#### 结束记录轨迹
接收到鼠标右键抬起时,切换状态机
重置之前维护的值
通过channel发送结束事件

#### 轨迹位移积累
维护 刚刚/现在的两套x,y坐标
维护 带符号的x,y累积变量(同轴反向时重置为当前增量,而非抵消)
垂直/水平阈值见 config 中 `方向阈值` 结构体(基础 100px, 水平倍率 2.0)
垂直优先判定; 各轴达阈值时只清零本轴,保留另一轴积累(支持 下右/上右 等多步轨迹)
达阈值后通过 channel 发送上下左右事件之一

**当前行为**：上述方向发送逻辑仍保留在 `monitor` 中，但已不再连接到动作匹配器；实际动作方向由 overlay 九宫格命中产生。

#### 普通右键单击补发
Hook 会吞掉右键按下/抬起。若本会话**未触发手势**（仅点开宫格或直接右键点击），overlay 在关闭浮窗并发送 `方向::结束` 后，由 `input/replay.rs` 延迟补发原位置右键单击（先等浮窗关闭，再分按下/抬起时序）。若已触发手势则不补发。

### 输出
通过channel向外发送监测事件:
- 开始: 右键按下
- 结束: 右键抬起
- 上/下/左/右: 轨迹方向（保留逻辑，当前不接匹配器）

## 动作模块

仅在 开始~结束 会话内处理方向。方向 channel 收到轨迹方向后追加到队列，按动作列表匹配前缀；队首无法匹配任何动作起点则丢弃。

动作配置集中在 `action/registry.rs` 的 `动作表`，每行包含：**名称**、**触发轨迹**、**快捷键**、**类型**。

### 动作类型

- **重复**：同一会话内可多次触发（未被锁定时）
- **独占**：触发一次后会话锁定，直到 结束 不再处理任何动作

### 当前动作

| 名称 | 轨迹 | 快捷键 | 类型 |
|------|------|--------|------|
| 关闭 | 下右 | Ctrl+W | 独占 |
| 恢复 | 上右 | Ctrl+Shift+T | 独占 |
| 左边 | 左 | Ctrl+Shift+Tab | 重复 |
| 右边 | 右 | Ctrl+Tab | 重复 |

多步轨迹在动作列表中靠前注册，避免被单步动作抢先匹配。

执行函数从执行 queue 取出按键序列并 SendInput 发送。

## Overlay 模块

Overlay 消费原始鼠标事件，负责九宫格 UI 与方向触发，不参与动作匹配。

### 交互
- 右键按下：展开 3×3 宫格，发送 `方向::开始`
- 移入上/下/左/右格：立即发送对应方向，以该格为新中心重建宫格
- 触发动作后，该格显示动作**名称**；成为中心后仍显示；移入新格后旧格恢复方向符号
- 四角为空白，不触发方向；中心格不触发方向
- 右键抬起：关闭浮窗，发送 `方向::结束`；未触发手势时补发右键单击

### 视觉
- 浅色扁平玻璃风格圆角矩形
- 保留格：标签 morph 动画（方向符号 ↔ 动作名称）
- 范围外格：淡出消失
- 新格：按移动方向逐个淡入
- 鼠标轨迹可选显示，由 `config::显示鼠标轨迹` 控制，默认关闭
- **tab 冻结层**（左/右，可 `启用冻结层` 关闭）：`PrintWindow` 截取前台窗口快照，按截屏窗口屏幕位置 1:1 像素绘制（`NEAREST`），超出 overlay 客户区裁剪；指数淡出，减轻 tab 硬切闪烁感

### tab 冻结层机制
- **缓存**：右键会话开始、每次方向触发后，统一延迟 `冻结缓存延迟毫秒`（默认 50ms）后台截屏；代际号丢弃过期任务，连切时不乱序覆盖
- **触发**：移入左/右格立即 SendInput；若已有缓存，先显示快照再淡出（不阻塞切换）
- **对齐**：记录截屏窗口 `GetWindowRect` 屏幕坐标，绘制时 `(截屏原点 - overlay原点) / DPI`，不拉伸铺满
- **淡出**：指数衰减（`平滑逼近`），时长由 `冻结淡出时长` 控制

### 配置
- `显示鼠标轨迹`：是否在 overlay 绘制鼠标轨迹，默认 `false`
- `启用冻结层`：tab 切换是否启用快照缓冲，默认 `true`
- `冻结淡出时长`：淡出约达阈值的时间（秒），默认 `0.5`
- `冻结缓存延迟毫秒`：动作/会话开始后延迟截屏时间，默认 `50`
- `冻结层峰值不透明度`：快照最高不透明度，默认 `0.9`
