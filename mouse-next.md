# MOUSE-NEXT 全局鼠标手势工具
鼠标手势在国产浏览器中是内置原生功能,但chrome原生没有,以及考虑到扩展实现的性能 效果不好(比如页面打开时 系统页面扩展没有输入响应)
所以需要一个软件,在系统层面检测鼠标按键和移动输入,获取鼠标轨迹,并转换成对应的键盘按键映射,通过按键映射或者扩展传递去控制实际页面
但实际上这个右键或绑定的其他的触发键的行为,是一个控制面板,来根据用户的输入意图映射为实际的控制动作

## 本文档说明
本文档用于记录开发需要的各种需求功能细节等,需要随时调整并确保所有信息的绝对准确
禁止AI主动修改本文档

# 功能模块

## 输入监测模块

注册WH_MOUSE_LL HOOK
接收到的事件带注入标志,跳过处理
接收到触发按键的事件,吞掉并处理(如鼠标右键)
接收到鼠标位移事件,处理但不吞掉

# 功能机制
- 动作执行,根据选择的功能执行对应动作
- 平滑渐变切换,鼠标离开格子后,截取窗口截屏,并替换显示为截屏,碰到新格子后执行动作并模糊渐变淡出

## 功能面板
- 右键按住展开,松手关闭(后续可能加入其他按键的切换展开关闭,架构上要支持)
- 浮动叠加界面,提供一个浮窗界面无焦点无边框来显示内容
- 九宫格排列 多个圆角矩形格子控件
- 目前动作是触碰执行,可选为松手执行,触碰执行的动作可选为按下再次触发(比如左右切换)

## 操作流程
右键按住,浮出显示展开面板,移动光标,触碰新格子,执行对应动作,右键松开,面板消失

## 动作
| 动作名称       | 快捷键           | 说明             | 触发方式 | 是否可重复 |
|:-------------|:----------------|:----------------|:-------|:----------|
| 切换左边     | Ctrl+Shift+Tab  | 切换到左侧标签页 | 触碰   | 是        |
| 切换右边     | Ctrl+Tab        | 切换到右侧标签页 | 触碰   | 是        |
| 关闭标签页   | Ctrl+W          | 关闭当前标签页   | 松手   | 否        |
| 恢复关闭标签页 | Ctrl+Shift+T    | 打开之前关闭标签页 | 松手   | 否        |

## 界面效果

展开位置9宫格 光标经过的格子高亮,执行动作会变成绿色高亮然后变回来
